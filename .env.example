# ==========================================
# BOOKING SYSTEM CONFIGURATION
# ==========================================

# ==========================================
# EMAIL CONFIGURATION (Required for email notifications)
# ==========================================

# Choose your email service: 'gmail' or 'smtp'
EMAIL_SERVICE=gmail

# For Gmail (Recommended for easy setup)
EMAIL_USER=nutricionistvladimir@gmail.com
EMAIL_APP_PASSWORD=your_16_character_app_password_here

# Email recipient for booking notifications
NUTRITIONIST_EMAIL=nutricionistvladimir@gmail.com

# Alternative: For custom SMTP servers (if not using Gmail)
# Uncomment and configure these if EMAIL_SERVICE=smtp
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_smtp_password

# ==========================================
# GOOGLE CALENDAR CONFIGURATION (Required for calendar integration)
# ==========================================

# Google OAuth2 Credentials (from Google Cloud Console)
GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_client_secret_here
GOOGLE_REFRESH_TOKEN=your_refresh_token_here

# Optional: Specify calendar ID (defaults to 'primary')
# GOOGLE_CALENDAR_ID=primary

# Optional: OAuth redirect URI (only if different from default)
# GOOGLE_REDIRECT_URI=https://developers.google.com/oauthplayground

# ==========================================
# OPTIONAL FEATURES
# ==========================================

# Enable availability checking before booking (true/false)
# If enabled, the API will check if the time slot is already taken
CHECK_AVAILABILITY=false

# ==========================================
# NOTES
# ==========================================
# 
# 1. DO NOT commit this file with real credentials to version control
# 2. Copy this file to .env.local for local development
# 3. Add environment variables to your hosting platform (Vercel, etc.)
# 4. All environment variables starting with NEXT_PUBLIC_ are exposed to the browser
# 5. Keep sensitive keys (like API keys) without NEXT_PUBLIC_ prefix
